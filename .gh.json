{
    "api_version": "2",
    "default_issue_component": {
        "LPS": "UI"
    },
    "default_issue_type": "Bug",
    "default_issue_version": {
        "LPS": "6.2.X"
    },
    "default_project": "LPS",
    "hooks": {
        "pull-request": {
            "close": {
                "before": [],
                "after": [
                    { "cmd": "ghd", "args": ["jira", "{{jira.number}}"] }
                ]
            },
            "fetch": {
                "before": [],
                "after": [
                    "{{#if jira.number}} ghd jira {{jira.number}} --comment 'Just started reviewing :)' {{/if}}",
                    { "cmd": "ghd", "args": ["jira", "{{jira.number}}"] }
                ]
            },
            "fwd": {
                "before": [],
                "after": [
                    "{{#if jira.number}} ghd jira {{jira.number}} --comment 'Pull request forwarded to {{forwardedLink}}. [See changes here.]({{compareLink}})' {{/if}}",
                    { "cmd": "ghd", "args": ["jira", "{{jira.number}}", "--github-user", "{{options.fwd}}", "--submittedLink", "{{forwardedLink}}"] }
                ]
            },
            "merge": {
                "before": [],
                "after": [
                    "{{#if jira.number}} ghd jira {{jira.number}} --comment 'Thank you, pull request merged! [See changes here.]({{compareLink}})' {{/if}}",
                    { "cmd": "ghd", "args": ["jira", "{{jira.number}}"] }
                ]
            },
            "open": {
                "before": [],
                "after": [
                    { "cmd": "ghd", "args": ["jira", "{{jira.number}}"] }
                ]
            },
            "submit": {
                "before": [],
                "after": [
                    "{{#if jira.number}} ghd jira {{jira.number}} --comment 'Pull request submitted to {{submittedLink}}. [See changes here.]({{compareLink}})' {{/if}}",
                    { "cmd": "ghd", "args": ["jira", "{{jira.number}}", "--github-user", "{{options.submit}}", "--submittedLink", "{{submittedLink}}"] }
                ]
            }
        }
    },
    "host": "issues.liferay.com",
    "password": "",
    "port": "",
    "protocol": "https",
    "transition": {
        "Reassign Review Request": {
            "Assignee": {
                "name": "{{jira.submit}}"
            },
            "Git Pull Request": "{{options.submittedLink}}"
        },
        "Start Progress": {
            "Assignee": {
                "name": "{{jira.user}}"
            }
        }
    },
    "user": ""
}