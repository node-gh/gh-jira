{
    "api_version": "2",
    "default_issue_component": {
        "LPS": "UI"
    },
    "default_issue_type": "Bug",
    "default_issue_version": {
        "LPS": "6.2.X"
    },
    "hooks": {
        "pull-request": {
            "close": {
                "before": [],
                "after": [
                    { "cmd": "gh", "args": ["jira", "{{jira.number}}"] }
                ]
            },
            "fetch": {
                "before": [],
                "after": [
                    "{{#if jira.number}} gh jira {{jira.number}} --comment 'Just started reviewing :)' {{/if}}",
                    { "cmd": "gh", "args": ["jira", "{{jira.number}}"] }
                ]
            },
            "fwd": {
                "before": [],
                "after": [
                    "{{#if jira.number}} gh jira {{jira.number}} --comment 'Pull request forwarded to {{forwardedLink}}. [See changes here.]({{compareLink}})' {{/if}}",
                    { "cmd": "gh", "args": ["jira", "{{jira.number}}", "--assignee", "{{options.fwd}}", "--submittedLink", "{{forwardedLink}}"] }
                ]
            },
            "merge": {
                "before": [],
                "after": [
                    "{{#if jira.number}} gh jira {{jira.number}} --comment 'Thank you, pull request merged! [See changes here.]({{compareLink}})' {{/if}}",
                    { "cmd": "gh", "args": ["jira", "{{jira.number}}"] }
                ]
            },
            "submit": {
                "before": [],
                "after": [
                    "{{#if jira.number}} gh jira {{jira.number}} --comment 'Pull request submitted to {{submittedLink}}. [See changes here.]({{compareLink}})' {{/if}}",
                    { "cmd": "gh", "args": ["jira", "{{jira.number}}", "--assignee", "{{options.submit}}", "--submittedLink", "{{submittedLink}}"] }
                ]
            }
        }
    },
    "host": "issues.liferay.com",
    "password": "",
    "port": "",
    "protocol": "https",
    "transition": {
        "Close Issue": {
            "Fix Version/s": {
                "name": "{{options.version}}"
            }
        },
        "Create Review Request": {
            "Assignee": {
                "name": "{{options.assignee}}"
            },
            "Git Pull Request": "{{options.submittedLink}}"
        },
        "Open Issue": {
            "Assignee": {
                "name": "{{options.assignee}}"
            }
        },
        "Reassign Review Request": {
            "Assignee": {
                "name": "{{options.assignee}}"
            },
            "Git Pull Request": "{{options.submittedLink}}"
        },
        "Start Progress": {
            "Assignee": {
                "name": "{{jira.user}}"
            }
        }
    },
    "user": ""
}